<h1>New Game</h1>
<%= form_for @game do |f| %>
  <%= f.label :court %>
  <%= f.collection_select(:court_id, Court.sort_alphabetically, :id, :name) %>
  <%= f.label :date %>
  <%= f.date_field :date %>
  <%= f.label :time %>
  <%= f.text_field :time%>
  <%= f.submit %>
<% end %>